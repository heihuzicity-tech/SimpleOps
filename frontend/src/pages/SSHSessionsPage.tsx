import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const SSHSessionsPage: React.FC = () => {
  return (
    <Card>
      <Title level={3}>SSH会话管理</Title>
      <p>SSH会话管理功能开发中...</p>
    </Card>
  );
};

export default SSHSessionsPage; 