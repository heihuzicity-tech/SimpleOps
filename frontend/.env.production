# Production environment configuration
# 使用Docker Compose部署时，不需要设置REACT_APP_API_URL
# nginx会自动代理所有/api/和/ws/请求到后端

# 仅在以下情况需要设置：
# 1. 前后端分离部署（不使用nginx代理）
# 2. 需要直接访问后端API
# 
# 示例：
# REACT_APP_API_URL=http://api.example.com:8080